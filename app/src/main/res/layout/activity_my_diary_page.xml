<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginStart="10dp"
    android:layout_marginTop="10dp"
    android:layout_marginEnd="10dp"
    android:layout_marginBottom="10dp"
    android:background="@drawable/diarypage"
    tools:context=".Schedule.MyDiaryPage">

    <TextView
        android:id="@+id/mydiarypage_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="35dp"
        android:text=""
        android:textSize="18sp"
        app:layout_constraintStart_toStartOf="@+id/mydiarypage_image"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/mydiarypage_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Title"
        android:textSize="24sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@+id/mydiarypage_image"
        app:layout_constraintTop_toBottomOf="@+id/mydiarypage_time" />

    <ImageView
        android:id="@+id/mydiarypage_image"
        android:layout_width="320dp"
        android:layout_height="200dp"
        android:layout_marginTop="24dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/mydiarypage_title"
        app:srcCompat="@drawable/photobox_empty" />

    <ImageButton
        android:id="@+id/mydiarypage_editbtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="40dp"
        android:background="@color/WHITE"
        app:layout_constraintEnd_toEndOf="@+id/mydiarypage_image"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/btn_edit" />

    <TextView
        android:id="@+id/mydiarypage_contents_text"
        android:layout_width="320dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:maxLines="15"
        android:scrollbars="vertical"
        android:text="contents"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="@+id/mydiarypage_image"
        app:layout_constraintStart_toStartOf="@+id/mydiarypage_image"
        app:layout_constraintTop_toBottomOf="@+id/mydiarypage_image" />

</androidx.constraintlayout.widget.ConstraintLayout>