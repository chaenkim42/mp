<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:hardwareAccelerated="true"
        >
        <activity android:name=".Schedule.ScheduleForm">

        </activity>
        <activity android:name=".Search.SearchMap" >

        </activity>
        <activity android:name=".Search.SearchPage" />
        <activity android:name=".Splash">

        </activity>
        <activity android:name=".Main.Main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AskAccount" />

        <meta-data
            android:name="com.kakao.sdk.AppKey"
            android:value="c0e9c3217878bcdc7abdbfb402ae0577" />

    </application>

</manifest>